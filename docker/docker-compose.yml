services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: agenticrag
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata_agenticrag:/var/lib/postgresql/data
volumes:
  pgdata_agenticrag:
